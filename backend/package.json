{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "node": "NODE_ENV=development && node --watch index.js",
    "test": "NODE_ENV=test && node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "check": "npm run test && npm run node"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "express": "^4.21.2",
    "express-async-handler": "^1.2.0",
    "passport": "^0.7.0",
    "passport-google-oidc": "^0.1.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prisma": "^6.3.1",
    "supertest": "^7.0.0"
  }
}
